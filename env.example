# ============================================================
# KD1 Docker Compose 環境設定
# ============================================================
# 使い方:
#   1. このファイルを .env にコピー: cp .env.example .env
#   2. 必要な箇所だけ変更する
#   3. docker compose up -d で起動
#
# ※ .env を作成しなくてもデフォルト値で動作します
# ============================================================

# ------------------------------------------------------------
# MySQL
# ------------------------------------------------------------
# MYSQL_VERSION=8.0
# MYSQL_PORT=3307
# MYSQL_ROOT_PASSWORD=password
# MYSQL_DATABASE=kd1
# MYSQL_USER=kd1
# MYSQL_PASSWORD=kd1

# ------------------------------------------------------------
# MongoDB
# ------------------------------------------------------------
# MONGO_VERSION=7.0
# MONGO_PORT=27017
# MONGO_USER=kd1
# MONGO_PASSWORD=kd1
# MONGO_DATABASE=kd1

# ------------------------------------------------------------
# MinIO (S3互換オブジェクトストレージ)
# アバター等は署名なし・有効期限なしの公開 URL で返す（public バケットは匿名読み取り）
# ------------------------------------------------------------
# MINIO_VERSION=latest
# MINIO_API_PORT=9000
# MINIO_CONSOLE_PORT=9001
# MINIO_ROOT_USER=kd1admin
# MINIO_ROOT_PASSWORD=kd1admin1234
# MINIO_MC_VERSION=latest
# サーバー用（アップロード接続先）
# MINIO_ENDPOINT=localhost
# MINIO_PORT=9000
# MINIO_BUCKET=public
# 公開 URL のベース（アップロード後の URL 組み立て用。未指定時は MINIO_ENDPOINT:PORT から組み立て）
# MINIO_PUBLIC_URL_BASE=http://localhost:9000
