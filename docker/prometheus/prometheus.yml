global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # コンテナメトリクス（メモリ・CPU）
  - job_name: 'cadvisor'
    static_configs:
      - targets: ['cadvisor:8080']

  # TODO: y-websocketにprom-client導入後に有効化
  # - job_name: 'yjs-server'
  #   static_configs:
  #     - targets: ['kd1-yjs-server:9091']
